export const GROUP = '/api'

export const linkConst = {
    MAIN: {
        INDEX: `${GROUP}/main`
    },

    CIRCLE: {
        GROUP: `${GROUP}/circle`
    },

    NEWJOY: {
        GROUP: `${GROUP}/newjoy`
    }
} as const